name: Test basic - ggshield secret scan ci requires API key
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run ggshield action without API key and expect failure
        id: action
        uses: .
        continue-on-error: true
        env:
          GITGUARDIAN_API_KEY: ""
      - name: Assert action failed without API key
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail without GITGUARDIAN_API_KEY, got outcome=$OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed without API key"
